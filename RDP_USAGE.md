# Использование RDP в sshgo

## 📋 Установка RDP клиентов

### xfreerdp (рекомендуется)

```bash
# Ubuntu/Debian
sudo apt-get install freerdp2-x11

# Fedora/RHEL
sudo yum install freerdp
```

### rdesktop (альтернатива)

```bash
# Ubuntu/Debian
sudo apt-get install rdesktop

# Fedora/RHEL
sudo yum install rdesktop
```

---

## 🚀 Использование

### Добавление RDP сервера

```bash
sshgo add
```

При добавлении выберите:
- **Тип подключения:** `2` (RDP)
- **Порт:** `3389` (стандартный порт RDP, можно изменить)

**Пример:**
```
Тип подключения:
  1) SSH (по умолчанию)
  2) RDP
Выберите тип [1]: 2

📝 Имя сервера: windows-server
🌐 Хост (IP/домен): 192.168.1.20
🚪 Порт [3389]: 
👤 Пользователь: Administrator
🔐 Пароль: ...
📋 Дополнительные RDP параметры [необязательно]: 
```

### Подключение к RDP серверу

```bash
# По имени
sshgo windows-server

# Через меню
sshgo
# Выберите сервер из списка
```

---

## 📝 Формат конфига

```
name|type|host|port|username|password|extra_params
```

**Примеры:**

```
# SSH сервер
linux-server|ssh|192.168.1.10|22|root|pass123|

# RDP сервер
windows-server|rdp|192.168.1.20|3389|Administrator|pass456|

# RDP с дополнительными параметрами
workstation|rdp|10.0.0.5|3389|user||/cert:ignore /dynamic-resolution /multimon
```

---

## 🔧 Дополнительные параметры RDP

### Для xfreerdp:

```
/cert:ignore              # Игнорировать сертификат
/dynamic-resolution       # Динамическое разрешение
/multimon                 # Поддержка нескольких мониторов
/sound:sys:alsa           # Звук через ALSA
/clipboard                # Общий буфер обмена
```

**Пример:**
```
workstation|rdp|10.0.0.5|3389|user|pass|/cert:ignore /dynamic-resolution /multimon
```

### Для rdesktop:

```
-g 1920x1080              # Разрешение экрана
-r sound:local            # Звук
-r clipboard:PRIMARYCLIPBOARD  # Буфер обмена
```

**Пример:**
```
workstation|rdp|10.0.0.5|3389|user|pass|-g 1920x1080 -r sound:local
```

---

## 🎯 Автоматический выбор клиента

Программа автоматически выбирает доступный RDP клиент в следующем порядке:
1. **xfreerdp** (приоритет)
2. **rdesktop** (fallback)

Если ни один клиент не найден, программа выведет инструкции по установке.

---

## 📋 Примеры использования

### Базовое подключение

```bash
sshgo windows-server
```

### Подключение через меню

```bash
sshgo
# Выберите сервер из списка:
# 1) linux-server [SSH]
# 2) windows-server [RDP]
```

### Просмотр информации о сервере

```bash
sshgo show windows-server
```

**Вывод:**
```
📋 Информация о сервере: windows-server

🔌 Тип подключения: RDP
🌐 Хост: 192.168.1.20
🚪 Порт: 3389
👤 Пользователь: Administrator
🔐 Пароль: установлен
📋 Параметры: -
🔗 Команда подключения: xfreerdp /v:192.168.1.20:3389 /u:Administrator
```

---

## ⚠️ Примечания

1. **Пароли:** RDP клиенты могут запрашивать пароль интерактивно, даже если он указан в конфиге
2. **GUI:** RDP требует графическое окружение (X11/Wayland)
3. **Безопасность:** Пароли хранятся в открытом виде в конфиге. Используйте права доступа `chmod 600 ~/.config/sshgo/connections.conf`

---

## 🔍 Устранение проблем

### Ошибка: "RDP клиент не найден"

**Решение:** Установите один из клиентов:
```bash
sudo apt-get install freerdp2-x11
# или
sudo apt-get install rdesktop
```

### Ошибка подключения

**Проверьте:**
1. Доступность хоста: `ping 192.168.1.20`
2. Открыт ли порт: `telnet 192.168.1.20 3389`
3. Правильность учетных данных
4. Настройки файрвола на RDP сервере

### Проблемы с разрешением экрана

**Для xfreerdp:** Добавьте параметр `/dynamic-resolution` в `extra_params`

**Для rdesktop:** Укажите разрешение: `-g 1920x1080` в `extra_params`

