# Руководство по миграции с Bash на Python версию

## 🔍 Текущая ситуация

У вас установлена **Bash версия** SSH Connection Manager в `/usr/local/bin/sshgo`.

## ❓ Нужно ли удалять Bash версию?

### ✅ **Рекомендуется удалить** перед установкой Python версии

**Причины:**
1. **Конфликт команд** - обе версии создают команду `sshgo`
2. **Избежание путаницы** - какая версия будет использоваться зависит от порядка в PATH
3. **Чистая установка** - проще управлять одной версией

### ⚠️ **Но можно оставить обе версии**

Если хотите протестировать Python версию перед полным переходом:
- Python версия обычно устанавливается в `~/.local/bin/sshgo`
- Bash версия в `/usr/local/bin/sshgo`
- Порядок в PATH определит, какая версия будет использоваться

## 🔄 Важно: Конфигурация общая!

**Хорошая новость:** Обе версии используют **один и тот же конфиг**:
```
~/.config/sshgo/connections.conf
```

Это означает:
- ✅ Ваши серверы сохранятся
- ✅ Не нужно мигрировать данные
- ✅ Можно переключаться между версиями

## 📋 Варианты миграции

### Вариант 1: Полная замена (рекомендуется)

```bash
# 1. Удаляем Bash версию
cd /home/azver/BachProjects
sudo ./install_sshgo.sh uninstall

# 2. Устанавливаем Python версию
cd sshgo_python
python3 install.py

# 3. Перезагружаем терминал
source ~/.bashrc
```

### Вариант 2: Тестирование Python версии

```bash
# 1. Устанавливаем Python версию (не удаляя Bash)
cd sshgo_python
python3 install.py

# 2. Проверяем, какая версия используется
which sshgo
sshgo --version  # или просто sshgo help

# 3. Если Python версия работает хорошо, удаляем Bash:
cd /home/azver/BachProjects
sudo ./install_sshgo.sh uninstall

# 4. Перезагружаем терминал
source ~/.bashrc
```

### Вариант 3: Временное использование Python версии

```bash
# 1. Устанавливаем Python версию
cd sshgo_python
python3 install.py

# 2. Используем Python версию напрямую
~/.local/bin/sshgo list

# 3. Или создаем алиас для тестирования
alias sshgo-py='~/.local/bin/sshgo'
sshgo-py list
```

## 🔍 Как проверить, какая версия используется?

```bash
# Проверка пути к команде
which sshgo

# Проверка типа файла
file $(which sshgo)

# Bash версия покажет: "Bourne-Again shell script"
# Python версия покажет: "Python script" или будет в ~/.local/bin/
```

## ⚙️ Настройка PATH (если нужно)

Если хотите, чтобы Python версия имела приоритет:

```bash
# Добавьте в ~/.bashrc (обычно уже есть после установки Python версии)
export PATH="$HOME/.local/bin:$PATH"
```

## 🗑️ Удаление Bash версии

Если решили удалить Bash версию:

```bash
cd /home/azver/BachProjects
sudo ./install_sshgo.sh uninstall
```

Или вручную:

```bash
# Удаляем скрипт
sudo rm -f /usr/local/bin/sshgo

# Удаляем автодополнение (если не нужно)
rm -f ~/.bash_completion

# Удаляем строки из .bashrc (вручную)
nano ~/.bashrc
# Удалите блоки с комментариями "SSH Connection Manager"
```

**⚠️ ВАЖНО:** Конфиг `~/.config/sshgo/connections.conf` **НЕ удаляется** - ваши серверы сохранятся!

## ✅ Рекомендация

**Для чистой установки:**
1. Удалите Bash версию
2. Установите Python версию
3. Проверьте работу

**Для тестирования:**
1. Установите Python версию
2. Протестируйте
3. Если все работает - удалите Bash версию

## 🔄 Откат (если нужно вернуться к Bash)

Если Python версия не подошла:

```bash
# Удаляем Python версию
cd sshgo_python
python3 install.py uninstall

# Или
pip3 uninstall sshgo

# Устанавливаем Bash версию обратно
cd /home/azver/BachProjects
sudo ./install_sshgo.sh
```

---

**Итог:** Удаление Bash версии **рекомендуется**, но **не обязательно**. Конфигурация общая, данные не потеряются.

