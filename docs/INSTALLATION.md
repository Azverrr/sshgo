# Установка SSH Connection Manager

## 📦 Требования

- **Python 3.6 или выше** (проверьте: `python3 --version`)
- **pip** (обычно уже установлен вместе с Python)
- **Linux** (Debian/Ubuntu, Fedora/RHEL/CentOS или другие дистрибутивы)

## 🚀 Быстрая установка

```bash
cd sshgo_python
python3 install.py
source ~/.bashrc  # или source ~/.zshrc для ZSH
```

**Примечание:** Установка использует флаг `--user` для обхода ограничения "externally-managed-environment" в современных Linux дистрибутивах (Debian/Ubuntu). Пакет будет установлен в `~/.local/bin/`.

## 🐚 Поддержка оболочек

SSH Connection Manager поддерживает **Bash** и **ZSH**.

### Автоматическая настройка

При установке программа автоматически:
- ✅ Определяет доступные оболочки (проверяет наличие `.bashrc` и `.zshrc`)
- ✅ Настраивает completion для всех найденных оболочек

**Пример:** Если у вас есть и `.bashrc`, и `.zshrc`, настройки будут добавлены в оба файла. Это означает, что вы можете переключаться между оболочками без дополнительной настройки.

### Переключение между оболочками

Если вы установили программу в одной оболочке, а потом переключились на другую:

1. **Автоматически:** Если обе оболочки были настроены при установке, всё уже готово.

2. **Вручную:** Используйте команду для переустановки completion:
   ```bash
   sshgo setup-completion
   ```
   Команда автоматически определит текущую оболочку и настроит completion.

### Настройка для конкретной оболочки

**Bash:**
- Настройки добавляются в `~/.bashrc`
- Completion работает через стандартный bash completion

**ZSH:**
- Настройки добавляются в `~/.zshrc`
- Используется `bashcompinit` для совместимости с bash completion скриптами
- Автоматически добавляется: `autoload -U +X bashcompinit && bashcompinit`

## 📋 Альтернативные способы установки

### Установка через pip

```bash
cd sshgo_python
pip3 install --user -e .
```

### Установка через DEB пакет

См. [BUILDING.md](BUILDING.md#deb-пакет)

### Установка через RPM пакет

См. [BUILDING.md](BUILDING.md#rpm-пакет)

## ⚙️ Настройка после установки

### Автоматическая настройка

Скрипт `install.py` автоматически:
- ✅ Устанавливает пакет
- ✅ Создает completion скрипт
- ✅ Настраивает completion для Bash (если есть `.bashrc`)
- ✅ Настраивает completion для ZSH (если есть `.zshrc`)
- ✅ Создает конфиг файл

### Ручная настройка (если нужно)

**Для Bash:**

1. **Добавить в PATH:**
   ```bash
   echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc
   ```

2. **Настроить автодополнение:**
   ```bash
   echo 'if [ -f ~/.bash_completion.d/sshgo-completion.sh ]; then' >> ~/.bashrc
   echo '    source ~/.bash_completion.d/sshgo-completion.sh' >> ~/.bashrc
   echo 'fi' >> ~/.bashrc
   ```

3. **Перезагрузить терминал:**
   ```bash
   source ~/.bashrc
   ```

**Для ZSH:**

1. **Добавить в PATH:**
   ```bash
   echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.zshrc
   ```

2. **Настроить автодополнение:**
   ```bash
   echo 'autoload -U +X bashcompinit && bashcompinit' >> ~/.zshrc
   echo 'if [ -f ~/.bash_completion.d/sshgo-completion.sh ]; then' >> ~/.zshrc
   echo '    source ~/.bash_completion.d/sshgo-completion.sh' >> ~/.zshrc
   echo 'fi' >> ~/.zshrc
   ```

3. **Перезагрузить терминал:**
   ```bash
   source ~/.zshrc
   ```

## 📝 Первая настройка

1. Создайте или отредактируйте конфиг:
   ```bash
   nano ~/.config/sshgo/connections.conf
   ```

2. Добавьте серверы в формате:
   ```
   server1|ssh|192.168.1.10|22|user|password|
   server2|ssh|example.com|2222|admin||
   ```

3. Проверьте:
   ```bash
   sshgo list
   ```

## 🔍 Проверка установки

```bash
# Проверка команды
which sshgo

# Проверка версии
sshgo help

# Проверка автодополнения
sshgo <Tab><Tab>
```

## 🗑️ Удаление

```bash
cd sshgo_python
python3 install.py uninstall
```

Скрипт удаления автоматически:
- ✅ Удаляет пакет
- ✅ Удаляет completion скрипт
- ✅ Очищает настройки из `.bashrc` (если существует)
- ✅ Очищает настройки из `.zshrc` (если существует)

**Или вручную:**
```bash
pip3 uninstall sshgo
rm ~/.bash_completion.d/sshgo-completion.sh
# Удалите строки из ~/.bashrc и ~/.zshrc вручную
```

## ❓ Решение проблем

См. [TROUBLESHOOTING.md](TROUBLESHOOTING.md)

