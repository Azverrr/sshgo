# Установка SSH Connection Manager

## 📦 Требования

### Обязательные зависимости

- **Python 3.6 или выше** (проверьте: `python3 --version`)
- **pip** (обычно уже установлен вместе с Python)
- **argcomplete** - для автодополнения команд (устанавливается автоматически при установке программы)
- **Linux** (Debian/Ubuntu, Fedora/RHEL/CentOS или другие дистрибутивы)

### Опциональные зависимости

Эти зависимости нужны для расширенной функциональности:

- **sshpass** - для SSH подключений с паролем (без него пароли не будут работать)
- **freerdp2-x11** или **rdesktop** - для RDP подключений к Windows серверам

### Установка зависимостей

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install python3 python3-pip
# Опционально для расширенной функциональности:
sudo apt-get install sshpass freerdp2-x11
```

**Fedora/RHEL/CentOS:**
```bash
sudo yum install python3 python3-pip
# Опционально для расширенной функциональности:
sudo yum install sshpass freerdp
```

**Arch Linux:**
```bash
sudo pacman -S python python-pip
# Опционально:
sudo pacman -S sshpass freerdp
```

**Примечание:** `argcomplete` устанавливается автоматически:
- При установке через `install.py` или `pip install` - из `install_requires` в `setup.py`
- При установке через DEB/RPM пакеты - как зависимость пакета (`python3-argcomplete` указан в `Depends`/`Requires`)

### Проверка зависимостей

```bash
# Проверка Python
python3 --version  # Должно быть 3.6 или выше

# Проверка pip
pip3 --version

# Проверка argcomplete
python3 -c "import argcomplete; print('OK')"  # Должно вывести "OK"

# Проверка опциональных зависимостей
which sshpass      # Для SSH с паролями
which xfreerdp     # Для RDP (xfreerdp)
which rdesktop     # Для RDP (альтернатива)
```

## 🚀 Быстрая установка

```bash
cd sshgo_python
sudo python3 install.py
```

**Примечание:** 
- Установка требует прав `sudo` (системная установка)
- Пакет устанавливается в системные директории:
  - `/usr/local/bin/sshgo` - исполняемый файл
  - `/usr/local/lib/python3.*/site-packages/sshgo/` - Python модули
  - `/usr/share/bash-completion/completions/sshgo` - скрипт автодополнения
- Автодополнение настраивается автоматически
- Всё работает сразу после установки, перезагрузка терминала не требуется

## 🐚 Поддержка оболочек

SSH Connection Manager поддерживает **Bash** и **ZSH** и работает одинаково в обеих оболочках без дополнительной настройки.

### Автоматическая настройка

При установке программа автоматически:
- ✅ Устанавливает completion скрипт в системную директорию `/usr/share/bash-completion/completions/sshgo`
- ✅ Скрипт автоматически загружается в Bash через стандартный механизм
- ✅ Скрипт автоматически загружается в ZSH через `bashcompinit` (если включен)

**Как это работает:**
- Completion скрипт устанавливается в системную директорию
- Bash автоматически загружает скрипты из `/usr/share/bash-completion/completions/`
- ZSH загружает их через `bashcompinit` (обычно уже включен в Oh My Zsh и других конфигах)
- Всё работает автоматически без дополнительной настройки

### Переключение между оболочками

Вы можете свободно переключаться между Bash и ZSH - completion работает в обеих оболочках без дополнительной настройки.

### Настройка для ZSH

Если автодополнение не работает в ZSH, убедитесь, что включен `bashcompinit` в конфигурации ZSH:

```bash
autoload -U +X bashcompinit && bashcompinit
```

Обычно это уже включено в популярных конфигах ZSH (Oh My Zsh, Prezto и т.д.).

### Ручная настройка completion

Если completion не работает, используйте команду:

```bash
sudo sshgo setup-completion
```

Команда пересоздаст completion скрипт в системной директории.

## 📋 Альтернативные способы установки

### Установка через pip (системная установка)

```bash
cd sshgo_python
sudo pip3 install .
```

**Зависимости:** `argcomplete` устанавливается автоматически из `setup.py`.

**Примечание:** Для системной установки через pip требуется `sudo`. Рекомендуется использовать `install.py` или пакетную установку (DEB/RPM).

### Установка через DEB пакет

См. [BUILDING.md](BUILDING.md#deb-пакет)

**Зависимости:** При установке DEB пакета `python3-argcomplete` устанавливается автоматически как зависимость пакета.

### Установка через RPM пакет

См. [BUILDING.md](BUILDING.md#rpm-пакет)

**Зависимости:** При установке RPM пакета `python3-argcomplete` устанавливается автоматически как зависимость пакета.

## ⚙️ Настройка после установки

### Автоматическая настройка

Скрипт `install.py` автоматически:
- ✅ Устанавливает пакет в системные директории
- ✅ Создает completion скрипт в `/usr/share/bash-completion/completions/sshgo`
- ✅ Настраивает completion для Bash и ZSH (автоматически, без правок конфигов)
- ✅ Создает конфиг файл `~/.config/sshgo/connections.conf`

**Никаких дополнительных действий не требуется!** Completion работает сразу после установки.

### Ручная настройка (если нужно)

**Обычно не требуется**, но если completion не работает:

1. **Проверьте, что скрипт существует:**
   ```bash
   ls -la /usr/share/bash-completion/completions/sshgo
   ```

2. **Для ZSH - убедитесь, что включен bashcompinit в конфигурации ZSH:**
   ```bash
   autoload -U +X bashcompinit && bashcompinit
   ```

3. **Пересоздайте completion скрипт:**
   ```bash
   sudo sshgo setup-completion
   ```

4. **Перезапустите терминал** для применения изменений

## 📝 Первая настройка

1. Создайте или отредактируйте конфиг:
   ```bash
   nano ~/.config/sshgo/connections.conf
   ```

2. Добавьте серверы в формате:
   ```
   server1|ssh|192.168.1.10|22|user|password|
   server2|ssh|example.com|2222|admin||
   ```

3. Проверьте:
   ```bash
   sshgo list
   ```

## 🔍 Проверка установки

```bash
# Проверка команды
which sshgo

# Проверка версии
sshgo help

# Проверка автодополнения
sshgo <Tab><Tab>
```

## 🗑️ Удаление

### Удаление через install.py

```bash
cd sshgo_python
python3 install.py uninstall
```

Скрипт удаления автоматически:
- ✅ Удаляет пакет из системных директорий
- ✅ Удаляет completion скрипт из `/usr/share/bash-completion/completions/sshgo`
- ✅ Очищает конфигурационные файлы (если нужно)

**Примечание:** Очистка конфигов оболочек не требуется.

### Удаление через пакетный менеджер

**DEB пакет:**
```bash
sudo apt remove sshgo
```

**RPM пакет:**
```bash
sudo rpm -e sshgo
```

### Ручное удаление

```bash
# Удалить пакет
sudo pip3 uninstall sshgo

# Удалить completion скрипт
sudo rm /usr/share/bash-completion/completions/sshgo

# Удалить конфиг (опционально)
rm -rf ~/.config/sshgo
```

## ❓ Решение проблем

См. [TROUBLESHOOTING.md](TROUBLESHOOTING.md)

