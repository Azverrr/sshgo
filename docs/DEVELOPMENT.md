# Разработка SSH Connection Manager

## 🏗️ Структура проекта

```
sshgo_python/
├── sshgo/              # Основной пакет
│   ├── cli.py         # CLI интерфейс
│   ├── config.py       # Управление конфигурацией
│   ├── connection.py   # SSH подключения
│   ├── menu.py         # Интерактивное меню
│   └── utils.py        # Утилиты
├── debian/             # DEB пакет
├── rpm/                # RPM пакет
│   └── sshgo.spec      # Spec файл
├── docs/               # Документация
└── ...
```

## 🔧 Разработка

### Установка для разработки

```bash
cd sshgo_python
pip3 install --user -e .
```

### Запуск из исходников

```bash
cd sshgo_python
python3 -m sshgo.cli
```

## 📊 Оптимизации

### Примененные оптимизации

1. **Оптимизированное чтение конфига**
   - `get_server()` - построчное чтение
   - `server_exists()` - быстрая проверка по имени
   - `get_server_names()` - чтение только имен

2. **Кэширование**
   - Кэш списка серверов для автодополнения
   - Проверка mtime для инвалидации

3. **Безопасность**
   - Атомарная запись конфига
   - Безопасная очистка экрана (ANSI коды)

4. **Парсинг**
   - Использование `shlex.split()` для параметров SSH

Подробнее: см. раздел "Оптимизации" ниже.

## 🧪 Тестирование

### Ручное тестирование

```bash
# Тест основных функций
sshgo list
sshgo add test-server 127.0.0.1 test test 22
sshgo show test-server
sshgo edit test-server
sshgo remove test-server
```

### Проверка автодополнения

```bash
source ~/.bash_completion.d/sshgo-completion.sh
sshgo <Tab><Tab>
```

## 📝 Код-стайл

- PEP 8 для Python кода
- Типизация (type hints)
- Docstrings для всех функций
- Обработка ошибок через try/except

## 🔍 Оптимизации (детально)

### config.py

- `get_server()`: O(n/2) вместо O(n)
- `server_exists()`: только проверка имени
- `get_server_names()`: чтение только имен

### cli.py

- Кэширование списка серверов
- Проверка mtime для инвалидации кэша

### menu.py

- ANSI escape коды вместо os.system

### connection.py

- shlex.split() для правильного парсинга параметров

## 🚀 Сборка пакетов

См. [BUILDING.md](BUILDING.md)

## 📚 Зависимости

- `argcomplete>=2.0.0` - для автодополнения (опционально)
- Стандартная библиотека Python

## 🔮 Будущие улучшения

- Логирование подключений
- Статистика использования
- Группировка серверов
- Поиск по серверам
- Экспорт/импорт конфигов (JSON/YAML)

