# Использование SSH Connection Manager

Это руководство поможет вам быстро начать работать с SSH Connection Manager.

## 🚀 Основные команды

### Интерактивное меню

```bash
sshgo
```

Откроется меню со списком серверов для выбора.

### Прямое подключение

```bash
sshgo server1
```

Подключается к серверу по имени.

### Список серверов

```bash
sshgo list
```

Показывает все доступные серверы.

## 🔧 Управление серверами

### Добавление сервера

**Интерактивно:**
```bash
sshgo add
```

**Быстрое добавление:**
```bash
sshgo add server1 192.168.1.10 root mypass 22
```

### Редактирование сервера

```bash
sshgo edit server1
```

### Удаление сервера

```bash
sshgo remove server1
```

### Информация о сервере

```bash
sshgo show server1
```

## ⌨️ Автодополнение

После установки доступно автодополнение:

```bash
sshgo <Tab><Tab>
```

Показывает список серверов для подключения.

**С префиксом:**
```bash
sshgo inv<Tab>
```

Автодополнит до `inv-edros`, `inv-prod` или `inv-test`.

### Настройка автодополнения

Если автодополнение не работает, используйте команду:

```bash
sshgo setup-completion
```

Команда автоматически:
- Определяет текущую оболочку (Bash или ZSH)
- Создает или обновляет completion скрипт
- Настраивает completion для текущей оболочки

**Полезно при:**
- Переключении между оболочками
- Переносе установки на другой компьютер
- Проблемах с автодополнением

## 📝 Конфигурация

### Формат конфига

Файл: `~/.config/sshgo/connections.conf`

```
# Format: name|type|host|port|username|password|extra_params
# SSH серверы
server1|ssh|192.168.1.10|22|user|mypassword|
server2|ssh|example.com|2222|admin||

# RDP серверы
windows-server|rdp|192.168.1.20|3389|Administrator|pass123|
workstation|rdp|10.0.0.5|3389|user||/cert:ignore /dynamic-resolution
```

### Параметры

| Поле | Описание | Пример |
|------|----------|--------|
| `name` | Имя подключения | `server1` |
| `type` | Тип подключения: `ssh` или `rdp` | `ssh`, `rdp` |
| `host` | IP или домен | `192.168.1.10` |
| `port` | Порт (22 для SSH, 3389 для RDP) | `22`, `3389` |
| `username` | Имя пользователя | `user` |
| `password` | Пароль (может быть пустым) | `mypassword` |
| `extra_params` | Дополнительные параметры | `-o ServerAliveInterval=60` (SSH) или `/cert:ignore` (RDP) |

### Безопасность

```bash
chmod 600 ~/.config/sshgo/connections.conf
```

## 🌐 Переменные окружения

### Изменить путь к конфигурационному файлу

По умолчанию используется: `~/.config/sshgo/connections.conf`

Чтобы использовать другой файл:
```bash
export SSH_CONFIG_FILE="$HOME/.config/ssh-connections.conf"
sshgo list
```

**Примечание:** Переменная должна быть установлена перед запуском команды `sshgo`.

## 🖥️ Поддержка RDP

SSH Connection Manager поддерживает подключения через RDP (Remote Desktop Protocol) для Windows серверов и других RDP-совместимых систем.

### Установка RDP клиентов

**xfreerdp (рекомендуется):**
```bash
sudo apt-get install freerdp2-x11
```

**rdesktop (альтернатива):**
```bash
sudo apt-get install rdesktop
```

### Добавление RDP сервера

При добавлении сервера выберите тип подключения:
```bash
sshgo add
```

Выберите:
- `1` для SSH (по умолчанию)
- `2` для RDP

**Пример добавления RDP сервера:**
```
Тип подключения:
  1) SSH (по умолчанию)
  2) RDP
Выберите тип [1]: 2

📝 Имя сервера: windows-server
🌐 Хост (IP/домен): 192.168.1.20
🚪 Порт [3389]: 
👤 Пользователь: Administrator
🔐 Пароль: ...
📋 Дополнительные RDP параметры [необязательно]: 
```

### Подключение к RDP серверу

```bash
sshgo windows-server
```

RDP подключения автоматически запускаются в фоновом режиме, не блокируя консоль. Окно RDP откроется независимо, и вы сможете продолжать работать в терминале.

### Дополнительные параметры RDP

**Для xfreerdp:**
```
/cert:ignore              # Игнорировать сертификат
/dynamic-resolution       # Динамическое разрешение
/multimon                 # Поддержка нескольких мониторов
```

**Пример конфига:**
```
workstation|rdp|10.0.0.5|3389|user|pass|/cert:ignore /dynamic-resolution /multimon
```

**Подробная документация:** [RDP_USAGE.md](../RDP_USAGE.md)

## 📋 Примеры

```bash
# Добавить SSH сервер
sshgo add prod-server 192.168.1.10 root mypass 22

# Добавить RDP сервер (интерактивно)
sshgo add
# Выберите тип: 2 (RDP)

# Подключиться к SSH
sshgo prod-server

# Подключиться к RDP
sshgo windows-server

# Редактировать
sshgo edit prod-server

# Удалить
sshgo remove prod-server
```

