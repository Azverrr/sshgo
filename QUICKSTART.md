# Быстрый старт - SSH Connection Manager (Python)

## 🚀 Установка

### Зависимости

Перед установкой убедитесь, что установлены зависимости:

**Обязательные:**
```bash
# Ubuntu/Debian
sudo apt-get install python3 python3-pip

# Fedora/RHEL
sudo yum install python3 python3-pip
```

**Опциональные (для расширенной функциональности):**
```bash
# Ubuntu/Debian
sudo apt-get install sshpass freerdp2-x11

# Fedora/RHEL
sudo yum install sshpass freerdp
```

**Примечание:** `argcomplete` устанавливается автоматически:
- При установке через `install.py` или `pip install` - из зависимостей проекта
- При установке через DEB/RPM пакеты - как зависимость пакета (`python3-argcomplete`)

### Установка программы

```bash
cd sshgo_python
sudo python3 install.py
```

**Примечание:** 
- Установка требует прав `sudo` (системная установка)
- Пакет устанавливается в системные директории
- Автодополнение настраивается автоматически
- Всё работает сразу после установки

## 📝 Первая настройка

### Добавление серверов

**Способ 1: Через команду (рекомендуется)**
```bash
sshgo add
```
Следуйте инструкциям на экране.

**Способ 2: Редактирование файла вручную**
```bash
nano ~/.config/sshgo/connections.conf
```

Добавьте серверы в формате (каждая строка - один сервер):
```
# SSH серверы
server1|ssh|192.168.1.10|22|user|password|
server2|ssh|example.com|2222|admin||

# RDP серверы
windows-server|rdp|192.168.1.20|3389|Administrator|pass123|
```

**Формат:** `имя|тип|хост|порт|пользователь|пароль|доп_параметры`

**Типы:** `ssh` (по умолчанию) или `rdp`

## 🎯 Использование

### Интерактивное меню
```bash
sshgo
```

### Прямое подключение
```bash
sshgo server1
```

### Список серверов
```bash
sshgo list
```

### Управление серверами

```bash
# Добавить сервер (интерактивно)
sshgo add
# При добавлении можно выбрать тип: SSH или RDP

# Быстрое добавление SSH
sshgo add server1 192.168.1.10 root mypass 22

# Редактировать
sshgo edit server1

# Удалить
sshgo remove server1

# Информация
sshgo show server1
```

### Поддержка RDP

Для подключения к Windows серверам через RDP:

1. **Установите RDP клиент:**
   ```bash
   sudo apt-get install freerdp2-x11
   ```

2. **Добавьте RDP сервер:**
   ```bash
   sshgo add
   # Выберите тип: 2 (RDP)
   ```

3. **Подключитесь:**
   ```bash
   sshgo windows-server
   ```

RDP подключения запускаются в фоне, не блокируя консоль.

### Автодополнение

После установки доступно автодополнение имен серверов:

```bash
sshgo <Tab><Tab>
```

Показывает список всех доступных серверов.

**С префиксом:**
```bash
sshgo inv<Tab>
```
Автодополнит до серверов, начинающихся с `inv` (например, `inv-test`, `inv-prod`).

## ❓ Помощь

```bash
sshgo help
```

## 🗑️ Удаление

**Через install.py:**
```bash
cd sshgo_python
python3 install.py uninstall
```

**Через пакетный менеджер:**
```bash
# DEB пакет
sudo apt remove sshgo

# RPM пакет
sudo rpm -e sshgo
```

