# Быстрый старт - SSH Connection Manager (Python)

## 🚀 Установка

```bash
cd sshgo_python
python3 install.py
```

**Примечание:** Установка использует флаг `--user`, что означает установку в `~/.local/bin/`. Это обходит ограничение "externally-managed-environment" в современных Linux дистрибутивах.

После установки перезагрузите терминал или выполните:
```bash
source ~/.bashrc
```

Если команда `sshgo` не найдена, убедитесь что `~/.local/bin` в PATH:
```bash
export PATH="$HOME/.local/bin:$PATH"
```

## 📝 Первая настройка

### Добавление серверов

**Способ 1: Через команду (рекомендуется)**
```bash
sshgo add
```
Следуйте инструкциям на экране.

**Способ 2: Редактирование файла вручную**
```bash
nano ~/.config/sshgo/connections.conf
```

Добавьте серверы в формате (каждая строка - один сервер):
```
server1|ssh|192.168.1.10|22|user|password|
server2|ssh|example.com|2222|admin||
```

**Формат:** `имя|ssh|хост|порт|пользователь|пароль|доп_параметры`

## 🎯 Использование

### Интерактивное меню
```bash
sshgo
```

### Прямое подключение
```bash
sshgo server1
```

### Список серверов
```bash
sshgo list
```

### Управление серверами

```bash
# Добавить сервер (интерактивно)
sshgo add

# Быстрое добавление
sshgo add server1 192.168.1.10 root mypass 22

# Редактировать
sshgo edit server1

# Удалить
sshgo remove server1

# Информация
sshgo show server1
```

### Автодополнение

После установки доступно автодополнение имен серверов:

```bash
sshgo <Tab><Tab>
```

Показывает список всех доступных серверов.

**С префиксом:**
```bash
sshgo inv<Tab>
```
Автодополнит до серверов, начинающихся с `inv` (например, `inv-test`, `inv-prod`).

## ❓ Помощь

```bash
sshgo help
```

## 🗑️ Удаление

```bash
cd sshgo_python
python3 install.py uninstall
```

