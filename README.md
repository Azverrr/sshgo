# SSH Connection Manager

Удобный менеджер SSH подключений с интерактивным меню и автодополнением.

## ✨ Возможности

- 🚀 **Быстрое подключение** - подключайтесь к серверам по имени
- 📋 **Интерактивное меню** - выбирайте сервер из удобного списка
- 🔧 **Автодополнение** - нажимайте Tab для автозавершения имен серверов
- 📝 **Простая настройка** - один конфигурационный файл
- 🔒 **Безопасность** - поддержка SSH ключей и паролей
- 🎨 **Красивый вывод** - цветные сообщения и статусы
- 🖥️ **Мультипротокол** - поддержка SSH и RDP протоколов

## 📦 Установка

### Зависимости

**Обязательные:**
- Python 3.6 или выше
- pip (обычно уже установлен вместе с Python)
- `argcomplete` - устанавливается автоматически при установке программы

**Опциональные (для расширенной функциональности):**
- `sshpass` - для SSH подключений с паролем
- `freerdp2-x11` или `rdesktop` - для RDP подключений

**Установка зависимостей:**

Ubuntu/Debian:
```bash
sudo apt-get update
sudo apt-get install python3 python3-pip
# Опционально:
sudo apt-get install sshpass freerdp2-x11
```

Fedora/RHEL:
```bash
sudo yum install python3 python3-pip
# Опционально:
sudo yum install sshpass freerdp
```

**Примечание:** 
- При установке через `install.py` или `pip install`: `argcomplete` устанавливается автоматически из `setup.py`
- При установке через DEB/RPM пакеты: `python3-argcomplete` устанавливается автоматически как зависимость пакета

**Быстрая установка:**
```bash
cd sshgo_python
sudo python3 install.py
```

**Подробная документация:** [docs/INSTALLATION.md](docs/INSTALLATION.md)

**Примечание:** 
- Установка требует прав `sudo` (системная установка)
- Пакет устанавливается в системные директории (`/usr/local/bin/`, `/usr/local/lib/python3.*/site-packages/`)
- Автодополнение настраивается автоматически в системной директории
- Всё работает автоматически без дополнительной настройки

**Поддержка оболочек:** Программа автоматически работает в **Bash** и **ZSH** без дополнительной настройки. Completion скрипт устанавливается в системную директорию `/usr/share/bash-completion/completions/sshgo` и автоматически загружается в обе оболочки. Подробнее: [docs/INSTALLATION.md](docs/INSTALLATION.md#-поддержка-оболочек)

## ⚙️ Настройка

### Конфигурационный файл

Конфигурационный файл создается автоматически при установке:
```
~/.config/sshgo/connections.conf
```

### Формат конфигурации

Каждая строка содержит один сервер в формате:
```
name|type|host|port|username|password|extra_params
```

**Примеры:**
```
# Комментарии начинаются с #
# SSH серверы
server1|ssh|192.168.1.10|22|user|mypassword|
server2|ssh|example.com|2222|admin||
local|ssh|localhost|22|user||
vps|ssh|my-vps.com|22|root|secretpass|-o ServerAliveInterval=60

# RDP серверы
windows-server|rdp|192.168.1.20|3389|Administrator|pass123|
workstation|rdp|10.0.0.5|3389|user||/cert:ignore /dynamic-resolution
```

**Параметры:**
- `name` - имя подключения (уникальное)
- `type` - тип подключения: `ssh` или `rdp`
- `host` - IP адрес или домен
- `port` - порт (22 для SSH, 3389 для RDP)
- `username` - имя пользователя
- `password` - пароль (может быть пустым для SSH ключей)
- `extra_params` - дополнительные параметры (опционально)

### Использование SSH ключей

SSH Connection Manager полностью поддерживает работу с SSH ключами. Это более безопасный способ подключения, чем использование паролей.

#### Автоматическое использование SSH ключей

Если поле `password` оставлено пустым, программа автоматически использует SSH ключи из стандартных мест (`~/.ssh/id_rsa`, `~/.ssh/id_ed25519` и т.д.).

**При добавлении сервера:**
```bash
sshgo add
```
При запросе пароля просто нажмите **Enter** (оставьте поле пустым).

**Пример конфига с SSH ключом:**
```
server1|ssh|192.168.1.10|22|user||
server2|ssh|example.com|2222|admin||
```

#### Указание конкретного SSH ключа

Если нужно использовать конкретный ключ, укажите его в поле `extra_params`:

**При добавлении сервера:**
```bash
sshgo add
```
В поле "Дополнительные SSH параметры" введите:
```
-i ~/.ssh/my_custom_key
```

**Пример конфига:**
```
server1|ssh|192.168.1.10|22|user||-i ~/.ssh/my_custom_key
server2|ssh|example.com|22|admin||-i ~/.ssh/prod_key -o ServerAliveInterval=60
```

### Безопасность

```bash
# Установите права доступа только для владельца
chmod 600 ~/.config/sshgo/connections.conf
```

## 🚀 Использование

**Основные команды:**
```bash
sshgo                    # Интерактивное меню
sshgo server1            # Подключение к серверу
sshgo list               # Список серверов
sshgo add                # Добавить сервер
sshgo edit server1       # Редактировать сервер
sshgo remove server1     # Удалить сервер
```

**Автодополнение:** После `sshgo ` + Tab показываются серверы для подключения. Настраивается автоматически при установке.

**Настройка completion:** Если автодополнение не работает, используйте `sudo sshgo setup-completion` для переустановки completion скрипта в системной директории.

**Поддержка протоколов:**
- **SSH** - подключения к Linux/Unix серверам
- **RDP** - подключения к Windows серверам (требует xfreerdp или rdesktop)

**Подробная документация:** [docs/USAGE.md](docs/USAGE.md) | [RDP_USAGE.md](RDP_USAGE.md)

## 📚 Документация

- **[Установка](docs/INSTALLATION.md)** - подробная инструкция по установке
- **[Использование](docs/USAGE.md)** - руководство пользователя
- **[RDP подключения](RDP_USAGE.md)** - работа с RDP протоколом
- **[Сборка пакетов](docs/BUILDING.md)** - сборка DEB и RPM пакетов
- **[Решение проблем](docs/TROUBLESHOOTING.md)** - типичные проблемы и решения
- **[Разработка](docs/DEVELOPMENT.md)** - для разработчиков

## 🗑️ Удаление

**Через install.py:**
```bash
cd sshgo_python
python3 install.py uninstall
```

**Через пакетный менеджер:**
```bash
# DEB пакет
sudo apt remove sshgo

# RPM пакет
sudo rpm -e sshgo
```

Подробнее: [docs/INSTALLATION.md](docs/INSTALLATION.md#удаление)

## 🆘 Поддержка

При возникновении проблем см. [docs/TROUBLESHOOTING.md](docs/TROUBLESHOOTING.md)

---

**Приятного использования!** 🚀

